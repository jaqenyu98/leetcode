class Solution {
public:
    vector<int> shuffle(vector<int>& nums, int n) {
        vector<int> rst;
        for(int i=0; i<n; ++i)
        {
            rst.push_back(nums[i]);
            rst.push_back(nums[i+n]);
        }
        return rst;
    }
};
