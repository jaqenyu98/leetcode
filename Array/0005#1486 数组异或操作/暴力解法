class Solution {
public:
    int xorOperation(int n, int start) {
        int rst = 0;
        for(int i = 0; i<n; ++i)
        {
            rst = rst^(start + 2*i);
        }
        return rst;
    }
};
