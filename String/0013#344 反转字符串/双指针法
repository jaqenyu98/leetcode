class Solution {
public:
    void reverseString(vector<char>& s) {
        //reverse(s.begin(), s.end());
        int front = 0, back = s.size() - 1;
        while(front<back)
        {
            swap(s[front], s[back]);
            ++front;
            --back;
        }
    }
};
//双指针法
