class Solution {
public:
    string toLowerCase(string str) {
        for(auto &s: str)
        {
            if(s<=90&&s>=65)
                s+=32;
        }
        return str;
    }
};
//ascii码大法好
